// App startup logic — runs before first render
// Extend with feature flag init, service initialization, etc.
export async function bootstrap(): Promise<void> {
  // Future: Tauri readiness checks, config loading, etc.
}
